// Build script - currently empty as we use proc-macro approach
fn main() {
    // UniFFI binding generation now uses proc-macro approach
    // The UDL file is kept for reference but not used in build
}
